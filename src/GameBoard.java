import java.awt.*;

public class GameBoard {

    public void update() {
        // Probably won't be updating GameBoard
    }

    public void draw(Graphics2D g2d) {
        g2d.setColor(Color.BLACK);
        g2d.drawRect(50, 50, 200,200);
    }
}
